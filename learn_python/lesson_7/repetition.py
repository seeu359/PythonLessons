# Реализуй функцию find_non_str, которая принимает на вход кортеж из
# n-элементов.
# Если все элементы в кортеже строки - верни None. В противном случае вывод
# должен таким, как в примере.
# Используя функцию is_string из /utils


# some_tuple = ('1', '2', 'some string, '....', 1)
# some_tuple2 = ('1', '2', 'some string, '....')

# find_non_str(some_tuple) # => Element with index 4 have no str type.
# find_non_str(some_tuple2) # => True
